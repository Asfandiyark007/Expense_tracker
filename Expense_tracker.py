import argparse
import os
import json

parser = argparse.ArgumentParser(
    prog="Expense Tracker",
    description="Track your expenses and income",
    epilog="Thank you for using Expense Tracker!",
)

parser.add_argument("-add", "--count")
parser.add_argument("-add", "--count")
parser.add_argument("-add", "--count")
parser.add_argument("-add", "--count")
parser.add_argument("-add", "--count")
parser.add_argument("-add", "--count")

args = parser.parse_args()
